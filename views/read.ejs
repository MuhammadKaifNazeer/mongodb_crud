<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/public/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 p-5 lg:p-10 flex items-start justify-center text-white pt-[5rem]"
    >
      <nav class="fixed top-5 rounded-full p-2 w-max border">
        <div class="flex items-center gap-2">
          <a
            class="px-3 py-1 flex items-center justify-center hover:bg-zinc-600 rounded duration-200 rounded-full"
            href="/"
            >Home</a
          >
          <a
            class="px-3 py-1 flex items-center justify-center hover:bg-zinc-600 rounded duration-200 rounded-full"
            href="/read"
            >Read</a
          >
        </div>
      </nav>
      <div class="w-full md:p-5 rounded-lg">
        <h1 class="text-white font-bold text-4xl text-start mb-5 w-full">
          All Users
        </h1>
        <div class="users grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
        <% if(users.length > 0){ %>
          <% users.forEach(function(user){%>
             <div class="user border border-zinc-800 duration-100 p-4 rounded-lg">
            <div class="flex items-center justify-start gap-2">
              <div class="w-[4rem] h-[4rem] rounded-full bg-zinc-700 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
              </div>
              <div class="flex flex-col items-start justify-center">
                <h3 class="text-xl tracking-tight font-bold">
                 <%= user.name %>
                </h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
              </div>
            </div>
            <div class="flex items-center gap-2 mt-3">
              <a
                class="px-3 py-1 w-full flex items-center justify-center bg-green-500 hover:bg-green-600 rounded duration-200"
                href="/edit/<%= user._id %>"
                >Update</a
              >
              <a
                class="px-3 py-1 w-full flex items-center justify-center bg-zinc-700 hover:bg-zinc-600 rounded duration-200"
                href="/delete/<%= user._id %>"
                >Delete</a
              >
            </div>
          </div>
          <% })  %>
        <% } else { %>
            <h3 class="text-zinc-500">no users yet</h3>
        <% } %>
        </div>
    </div>
  </body>
</html>
